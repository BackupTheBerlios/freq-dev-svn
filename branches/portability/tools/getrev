#!/bin/sh

if [ -n "$2" ]; then echo -n $2
else
 if [ -r ./VERSION ]; then python VERSION
 else
  echo -n $1.
  q=`svnversion -n`
  r=$?
  if [ $r -eq 0 ]; then echo -n $q; else echo -n 'dev'; fi
 fi
fi